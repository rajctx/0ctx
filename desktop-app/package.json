{
  "name": "@0ctx/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "0ctx Desktop — Tauri system tray companion",
  "scripts": {
    "tauri": "tauri",
    "dev": "node scripts/tauri-run.mjs dev --no-dev-server-wait",
    "build": "node scripts/tauri-run.mjs build",
    "build:debug": "node scripts/tauri-run.mjs build --debug",
    "smoke": "node scripts/smoke-build.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-updater": "^2",
    "@tauri-apps/plugin-shell": "^2"
  }
}
